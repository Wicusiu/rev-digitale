import { InstanceState } from './reducers';

const appInitialState: InstanceState = {
  application: {
    message: {
      entity: [],
    },
    brick: {
    },
    user: {
    },
  },
  routing: null,
};

export default appInitialState;
